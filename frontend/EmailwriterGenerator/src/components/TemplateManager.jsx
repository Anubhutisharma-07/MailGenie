import React from 'react';
import { Box, Typography } from '@mui/material';

export default function TemplateManager() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>Template Manager</Typography>
      <Typography variant="body1">
        Custom templates logic will be migrated here.
      </Typography>
    </Box>
  );
}
